#from tkinter import *
import tkinter as tk 
#from tkinter import scrolledtext

root = tk.Tk() 
root.geometry("600x500")  # SIZE OF APP WINDOW
root.title("CHAT APP")    # TITLE OF APP WINDOW
root.configure(bg = "black") # changes the background color 


	

# lbl = Label(root, text="Hello")

# lbl.pack(side = LEFT)
#w = Label(root, text ='GeeksForGeeks', 
		#font = "50") 

#w.pack() 


#mylist = Listbox(root, 
				#yscrollcommand = scroll_bar.set ) 

#for line in range(1, 26): 
	#mylist.insert(END, "Geeks " + str(line)) 

#mylist.pack( side = LEFT, fill = BOTH ) 

#scroll_bar.config( command = mylist.yview ) 
# T = StringVar()


#T1 = Text((root) ,height = 18, width=100)
#T1.pack(side = TOP)


def clicked():                                                   # clicked function defined here 
  # T1 = tk.Text((root) ,height = 18, width=100)
   #T1.pack(side = tk.TOP)
 
   retrievename = nameBox.get("1.0",tk.END)
   T1.insert(tk.INSERT,retrievename)
   T1.insert(tk.INSERT, "- ")

   retrieveinput  =  T.get("1.0",tk.END)       # should retrieve the text entered by user
   T1.insert(tk.INSERT, retrieveinput)         # clears the text entry box after each sent message
   T.delete("1.0",tk.END)

   
   

   
                        
def darkness():

 root.configure(bg = "black")
 root.configure(bg = "black")
 frame.configure(bg = "black")
 frame1.configure(bg = "black")
 frame2.configure(bg = "black")
 disconnectbtn.configure(bg= "black")
 lightbtn.configure(bg= "black")
 darkbtn.configure(bg= "black")
 connectbtn.configure(bg= "black")
 btn.configure(bg= "black")
 IPlabel.configure(bg= "black")
 portlabel.configure(bg= "black")
 namelabel.configure(bg= "black")
 T.configure(bg = 'white')
 T1.configure(bg = 'black')

 magentabtn.configure(bg = "black")
 greenbtn.configure(bg = "black")

def light():
  
	root.configure(bg = "cyan")
	frame.configure(bg = "cyan")
	frame1.configure(bg = "cyan")
	frame2.configure(bg = "cyan")
	disconnectbtn.configure(bg= "white")
	lightbtn.configure(bg= "white")
	darkbtn.configure(bg= "white")
	connectbtn.configure(bg= "white")
	btn.configure(bg= "white")
	IPlabel.configure(bg= "cyan")
	portlabel.configure(bg= "cyan")
	namelabel.configure(bg= "cyan")
	T1.configure(bg = 'white')
	T.configure(bg = 'white')
	magentabtn.configure(bg = "white")
	greenbtn.configure(bg = "white")
	

def magenta():
	disconnectbtn.configure(fg= "magenta")
	lightbtn.configure(fg= "magenta")
	darkbtn.configure(fg= "magenta")
	connectbtn.configure(fg= "magenta")
	btn.configure(fg= "magenta")
	IPlabel.configure(fg= "magenta")
	portlabel.configure(fg= "magenta")
	namelabel.configure(fg= "magenta")
	magentabtn.configure(fg = "magenta")
	greenbtn.configure(fg = "magenta")
	T1.configure(fg = 'magenta')

def green():
	disconnectbtn.configure(fg= "green")
	lightbtn.configure(fg= "green")
	darkbtn.configure(fg= "green")
	connectbtn.configure(fg= "green")
	btn.configure(fg= "green")
	IPlabel.configure(fg= "green")
	portlabel.configure(fg= "green")
	namelabel.configure(fg= "green")
	magentabtn.configure(fg = "green")
	greenbtn.configure(fg = "green")
	T1.configure(fg = 'green')
	
   

def connect(): # this is the function that will execute when the connect button is pressed
	# DELETE EVERYTHING AFTER THE PORTVALUE  AND ADD THE CODE TO CONNECT, 

	# when the connect button is pressed the IP and port numbers will be stored in IPValue and PORTValue 
   IPValue=IPBox.get("1.0","end-1c") # STORES THE IP VALUE
   PORTValue=PortBox.get("1.0","end-1c") # STORES THE PORT VALUE
   
   
   retrieveinput2  =  IPBox.get("1.0",tk.END)       
   T1.insert(tk.INSERT, retrieveinput2)         
   T.delete("1.0",tk.END)
   


   # this is the function that will execute when the disconnect button is pressed
def disconnect():
	# DELETE EVERYTHING WITHIN THIS FUNCTION AND ADD THE CODE TO DISCONNECT
 
  retrieveinput2  = IPBox.get("1.0",tk.END)       
  T1.insert(tk.INSERT, retrieveinput2)         
  T.delete("1.0",tk.END)
 

frame = tk.Frame(root,bg = "black")
frame.pack(side = tk.LEFT,anchor=tk.NW)

frame1 = tk.Frame(root,bg = "black")
frame1.pack(side = tk.LEFT,anchor=tk.N)

frame2 = tk.Frame(root,bg = "black")
frame2.pack(side = tk.RIGHT,anchor=tk.NE)

#frame2 = tk.Frame(root,bg = "black")
#frame2.pack(side = tk.TOP,anchor=tk.N)


nameBox=tk.Text(frame1, height=1, width=14)
nameBox.pack(side = tk.TOP,anchor = tk.N)

# BUTTON DEFINITIONS BELOW

btn = tk.Button(root, text=" Send ",fg = "green",bg = "black",command = clicked)
btn.pack(side = tk.RIGHT, anchor = tk.SW)
#btn.pack(side = tk.RIGHT, fill = tk.Y)
#btn.bind("<Return>", clicked)

IPBox=tk.Text(frame1, height=1, width=14)
IPBox.pack(side = tk.TOP,anchor = tk.N)

PortBox=tk.Text(frame1, height=1, width=10)
PortBox.pack(side = tk.TOP,anchor = tk.N)

disconnectbtn = tk.Button(frame1, text="  Disconnect    ",fg = "green",bg = "black",command = disconnect)
disconnectbtn.pack(side=tk.BOTTOM)
#disconnectbtn.pack(side = tk.TOP, fill = tk.Y)

connectbtn = tk.Button(frame1, text="     Connect     ",fg = "green",bg = "black",command = connect)
connectbtn.pack(side=tk.BOTTOM)

#Dark and lightmode buttons 

darkbtn = tk.Button(frame2, text="    Dark  ",fg = "green",bg = "black",command = darkness)
darkbtn.pack(side=tk.TOP)

lightbtn = tk.Button(frame2, text="   Light  ",fg = "green",bg = "black",command = light)
lightbtn.pack(side=tk.TOP)

greenbtn = tk.Button(frame2, text="    Green  ",fg = "green",bg = "black",command = green)
greenbtn.pack(side=tk.BOTTOM)

magentabtn = tk.Button(frame2, text="   Magenta  ",fg = "green",bg = "black",command = magenta)
magentabtn.pack(side=tk.BOTTOM)
#connectbtn.pack(side = tk.TOP, fill = tk.Y)

IPlabel = tk.Label(frame, text="IP           ",fg = "green",bg = "black")
IPlabel.pack(side=tk.BOTTOM, anchor = tk.NW)

portlabel= tk.Label(frame, text="PORT    ",fg = "green",bg = "black")
portlabel.pack(side=tk.BOTTOM, anchor = tk.NW)

namelabel = tk.Label(frame, text="USER    ",fg = "green",bg = "black")
namelabel.pack(side=tk.TOP, anchor = tk.NW)

#sbVerticalScrollBar.pack(fill=tk.Y, side=tk.RIGHT, expand=tk.FALSE)

T1 = tk.Text((root),height = 25, width=150,wrap=tk.WORD,bg = 'white') # this defines the box where text is displayed 
T1.pack(side = tk.TOP)
#T1.grid(column=0, columnspan=3)


#scroll_y = tk.Scrollbar(root, orient="vertical", command=T1.yview)
#scroll_y.pack(side=tk.RIGHT, expand=True, fill="y")

T = tk.Text ((root) ,height=4, width=100,bg = 'white')  # this defnes the box where the users type
T.pack(side = tk.BOTTOM)


#T1.configure(yscrollcommand=scroll_y.set)



root.mainloop() 

